FROM python:3.12-slim

WORKDIR /app

COPY requirements.txt .

RUN pip install --no-cache-dir -r requirements.txt

COPY streamlit ./streamlit

EXPOSE 10000

CMD ["sh", "-c", "streamlit run streamlit/app.py --server.address=0.0.0.0 --server.port=${PORT:-10000}"]